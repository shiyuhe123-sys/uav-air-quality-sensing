# Aircraft configuration record template

**Template status:** blank preparation and evidence-recording template. No physical flight result is recorded here.

Use one copy of this record for each configuration under test. Keep dates non-location-specific and use initials or a role for public records instead of personal contact details. Use `not installed`, `not measured`, `not recorded` or `pending` when a field does not apply or evidence is not yet available.

## Record identity

- Record identifier:
- Date (`YYYY-MM-DD`):
- Responsible operator or reviewer (initials or role):
- Purpose of configuration record:
- Record status: `[ ] draft` `[ ] complete` `[ ] review pending` `[ ] reviewed`

## Aircraft configuration

- Airframe:
- Flight controller:
- Firmware and version:
- Parameter-set identifier or checksum:
- Receiver and control link:
- GPS and compass configuration:
- Power module and interface:
- Battery chemistry:
- Battery cell count:
- Battery capacity:
- Battery condition at test (for example, new, storage, cycle count not recorded):
- Propeller size:
- Propeller rotation arrangement:
- Take-off mass:
- Centre-of-gravity assessment:
- Known deviations from the project baseline:

Use `not measured` or `not recorded` rather than estimating a value after the test.

## Sensor payload configuration

- Sensor-payload status: `[ ] not installed` `[ ] installed` `[ ] not recorded` `[ ] pending`
- Sensor or payload model:
- Sensor-mount revision:
- Inlet revision or description:
- Payload mass:
- Payload power source:
- Payload communication or logging interface:
- Payload storage identifier:
- Payload configuration deviations:

Do not describe an uninstalled payload as flight-tested. The final sensor mount remains pending in the current project record.

## Supporting evidence

- Parameter export or checksum reference:
- Configuration photographs or drawing reference (after metadata review):
- Mass measurement reference:
- Centre-of-gravity measurement reference:
- Battery reference:
- Payload/mount reference:
- Other evidence identifiers:

Raw logs and private files may be referenced by a non-sensitive identifier without being committed to this repository. Do not include precise test locations or private absolute paths.

## Review

- Reviewer (initials or role):
- Review date (`YYYY-MM-DD`):
- Review status: `[ ] not reviewed` `[ ] evidence incomplete` `[ ] reviewed with limitations` `[ ] approved for use in a test record`
- Review notes:

## Configuration limitations

List any values that remain unknown or any difference between the recorded configuration and the aircraft actually used. In particular, do not infer an exact PM08D CAN port, cable or parameter configuration; those details remain pending an exported as-built record.

- Limitations:
- Follow-up action:
